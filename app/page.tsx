import Image from "next/image";

export default function Home() {
  return (
    <div className="container mx-auto px-5">
      // openpoll is open source, it lets you create forms (self-host) for surveys with webhooks.
    </div>
  );
}
